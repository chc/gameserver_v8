/*function OnPlayerConnect(Player) {
	print("New Player: " Player.name);
	print("Player IP: " Player.IP);
	if(Player.client == 'SAMP') {
		print("User is connected via SAMP");
	}
}

GameServer.name = "West Coast Role Play";
GameServer.player_limit = 100;
GameServer.max_per_ip = 5;
GameServer.version = "4.0";
GameServer.on('PlayerConnect', OnPlayerConnect);
*/
GameServer.name = "The JS server lives";
GameServer.player_limit = 100;
//GameServer.max_per_ip = 5;
GameServer.version = "JSServ 0.1";